// if() is sued to check the conditions and accordingly display the code in it 

// <, >, <=, >=, ==, ===, !=, !==, &&, ||, ! 
// === is used to check the value and type both
// !== is used to check the value and type both

// const temperature = 41

// if(temperature === 40) {
//     console.log("It's a hot day");
// } else {
//     console.log("It's a mild day");
// }

// console.log(`It's ${temperature} degrees outside`);

// ***************************************************************************************************

// const score = 200
// if(score > 100){
//     let power = "fly"
//     console.log(`User power is ${power}`);
// }
// console.log(`User power is ${power}`);

// if the variable is declared using const or let inside the if block then it can't be accessed outside the block, 
// but if the variable is declared using var then it can be accessed outside the block which is not a good practice

// ***************************************************************************************************

 